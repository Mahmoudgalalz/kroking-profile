---
import Icon from './Icon.astro';
import ThemeToggle from './ThemeToggle.astro';
import type { iconPaths } from './IconPaths';
/** Main menu items */

const textLinks: { label: string; href: string }[] = [
	{ label: 'Home', href: '/' },
];

/** Icon links to social media — edit these with links to your profiles! */
// const iconLinks: { label: string; href: string; icon: keyof typeof iconPaths }[] = [
// 	{ label: 'Twitter', href: 'https://twitter.com/eitmg', icon: 'twitter-logo' },
// 	{ label: 'GitHub', href: 'https://github.com/mahmoudgalalz', icon: 'github-logo' },
// 	{ label: 'Linkedin', href: 'https://linkedin.com/in/mahmoudgalalz', icon: 'linkedin-logo' },
// ];

const links : { label: string; href: string}[] = [
	{
		label: 'Blogs',
		href: '/blogs'
	},
	{
		label: 'Projects',
		href: '/projects'
	},
	{
		label: 'About',
		href: '/'
	},
	{
		label: 'Shorts, Spaces',
		href: '/content'
	},
] 
---

<nav>

	<div class="my-10">
		<div class="font-bold text-white text-2xl">
			 Mahmoud Galal <span class="font-sm text-sm">aka KroKing</span>
		</div>
		<div class="flex justify-end relative" >
			<ul class="flex space-x-3">
				{
					links.map(({ label, href }) => (
						<li>
							<a
							transition:animate="slide"
								aria-current={Astro.url.pathname === href}
								class={`${Astro.url.pathname === href ? ' absolute font-bold left-0':''} `}
								href={href}
							>
								{label}
							</a>
						</li>
					))
				}
			</ul>
	
		</div>
	</div>

	
</nav>

